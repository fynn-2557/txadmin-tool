# txadmin-tool
One-click installer and updater for txAdmin (FiveM server panel) on Windows Server 2025. Automatically installs FXServer, MariaDB or MySQL, configures firewall rules, sets up a Windows service, and lets you update to the latest build with a single click — no winget or Chocolatey required.
